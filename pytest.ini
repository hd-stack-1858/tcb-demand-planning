[pytest]
markers =
    unit: pure-function / file-system tests, no credentials needed
    integration_mocked: uses mocked external calls, no credentials needed
    integration: requires real dev Supabase credentials (.env.dev)
